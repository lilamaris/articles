## 설치 미디어로 부팅
1. `Expert Install` 옵션으로 진행
2. `disk partitioning` 단계 전까지 시스템 옵션 설정
	- `Languages`, `Country`, `Locale`, `Timezone` 등 설정
	- 대부분의 경우 설치 미디어 스크립트가 첫 번째로 강조 표시해준 옵션을 선택해도 무방함
	- `User` 설정에서 `root` 로그인 허용 금지; 생성할 사용자는 자동으로 `sudo` 그룹이 추가됨
